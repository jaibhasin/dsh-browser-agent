1. Allow the browser agent to run sub-agents controlling and running multiple tabs.
2. Store past sessions with website links so that we can reopened later, or switched while doing other tasks (allowing paralle work, but not on the same tab
3. The tool calls should be displayed as a nice thread. (imrpove the ui )