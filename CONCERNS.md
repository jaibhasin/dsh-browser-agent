# Concerns

- Browser navigation and tab switching currently run without user confirmation, so the agent can unexpectedly change the active page. Add an approval gate for cross-site navigation and tab switches.
